import { prepareUrl } from "../../helpers/PrepareUrl";

const baseUrl = "https://api.yotpo.com/v1/widget/";

describe("PrepareUrl", () => {
  it("returns proper URL for Singlelang", () => {
    // expect(prepareUrl()).toBe(baseUrl + )
  });
});
